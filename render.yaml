services:
  - type: web
    name: ai-code-reviewer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run "ai code reviewer/app.py" --server.port $PORT
    pythonVersion: 3.10.12
